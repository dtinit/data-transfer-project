/**
 * A transfer operation.
 */
export class Transfer {
    /**
     * The unique transfer id.
     */
    id: string;

    /**
     * A URL to advance the state of the transfer operation.
     */
    link: string;
}